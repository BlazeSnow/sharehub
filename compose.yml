services:
  ftp:
    build:
      context: .
      dockerfile: Dockerfile.ftp
    image: blazesnow/sharehub:ftp
    container_name: ftp
    restart: no
    ports:
      - 21:21
    environment:
      - USERNAME=blazesnow
      - PASSWORD=password
    volumes:
      - ./data:/data
